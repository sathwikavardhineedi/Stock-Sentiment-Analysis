asyncpraw==7.6.2
pandas==2.1.1
xgboost==1.7.6
scikit-learn==1.3.1
vaderSentiment==3.3.2
matplotlib==3.8.0
numpy==1.24.3

Explanation:
asyncpraw: For scraping Reddit data asynchronously.
pandas: For data manipulation and DataFrame operations.
xgboost: For training and evaluating the XGBoost classifier.
scikit-learn: For preprocessing, model evaluation metrics, and utilities.
vaderSentiment: For sentiment analysis.
matplotlib: For visualizing results like confusion matrices (if needed).
numpy: Required for numerical operations (used internally by many libraries).
